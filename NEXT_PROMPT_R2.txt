这次 jq 浮点精度那个修对了，0.123456789012345678901234567890 进去原样出来，0.1+0.2 也对。

但还有两个原问题没动：
- jq 那个 `[?(...)]` 谓词过滤还是报"Expected ']' but found LPAREN"，`.users[?(@.age > 18)].name` 跑不通，等于过滤功能没做。
- 多文件加载的 `*` 通配符还是不行，`jj '.name' '/tmp/f*.json'` 把字面量当文件名处理，没展开 glob。

检查修复一下。
